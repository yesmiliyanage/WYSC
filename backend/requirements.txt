# ── Core web framework ────────────────────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.1
flasgger==0.9.7.1

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Database & Auth ───────────────────────────────────────────────────────────
supabase==2.7.4

# ── AI / LLM ─────────────────────────────────────────────────────────────────
# openai 1.x — there is NO public 2.x release; the previous requirements.txt
# referenced a non-existent version (2.16.0) that would fail pip install.
openai==1.52.0

# ── Maps ──────────────────────────────────────────────────────────────────────
googlemaps==4.10.0

# ── RAG pipeline (optional — not wired into live API flow) ────────────────────
# langchain 0.x — previous requirements.txt referenced non-existent 1.2.7.
# langchain-community and langchain-openai are required for the updated imports
# in rag/rag_engine.py (langchain deprecated its bundled integrations in 0.1+).
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.2.9
# faiss-cpu 1.13.2 does not exist; latest stable release is 1.8.x.
faiss-cpu==1.8.0
# numpy 2.4.1 does not exist; pin to a known-stable 1.x release.
numpy==1.26.4

# ── Production server ─────────────────────────────────────────────────────────
# gunicorn was missing entirely — required for deployment on Railway/Render/Heroku.
gunicorn==22.0.0
